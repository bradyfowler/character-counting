#brady

bruh = input("input a string")

characters = {}
#for every character in the string it checks for every letter
for i in bruh:
     for i in characters:
          characters(i) += 1
     else:
          characters(i) = 1
print(str(characters))
